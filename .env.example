# Copy to .env so the container runs as your host user (keeps bind-mounted
# files from being created root-owned). Generate the real values with:
#   printf 'PUID=%s\nPGID=%s\n' "$(id -u)" "$(id -g)" > .env
PUID=1000
PGID=1000
